@model IEnumerable<Continental.CUP.Repositories.ViewModels.ReaderKindVModel>
@{
    ViewData["Title"] = "ReaderKind";
}

<h2>Lista de Modelos de Reader</h2>
<p>
    <a asp-action="Create" class="btn btn-primary">Nuevo</a>
</p>
<table id="dt_basic" class="table table-striped table-bordered table-hover" width="100%">
    <thead>
        <tr>
            <th data-class="expand"><i class="fa fa-fw fa-user text-muted hidden-md hidden-sm hidden-xs"></i> @Html.DisplayNameFor(model => model.Brand)</th>
            <th data-hide="phone"><i class="fa fa-fw fa-user text-muted hidden-md hidden-sm hidden-xs"></i> @Html.DisplayNameFor(model => model.ModelName)</th>
            <th data-hide="phone"><i class="fa fa-fw fa-user text-muted hidden-md hidden-sm hidden-xs"></i> @Html.DisplayNameFor(model => model.AntennaNumber)</th>
            <th data-hide="phone"></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Brand)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ModelName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.AntennaNumber)
                </td>
                <td>
                    @if (Continental.CUP.Web.Classes.HelperAction.IsValid("ReaderKind", "Edit"))
                    {
                        <a asp-action="Edit" asp-route-id="@item.ReaderKindID">Editar</a> @Html.Raw("|")
                    }
                    @if (Continental.CUP.Web.Classes.HelperAction.IsValid("ReaderKind", "Details"))
                    {
                        <a asp-action="Details" asp-route-id="@item.ReaderKindID">Detalles</a> @Html.Raw("|")
                    }
                    @if (Continental.CUP.Web.Classes.HelperAction.IsValid("ReaderKind", "Delete"))
                    {
                        <a asp-action="Delete" asp-route-id="@item.ReaderKindID">Eliminar</a>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>

@section Header {
    <link href="~/js/plugin/datatables/update/css/buttons.dataTables.min.css" rel="stylesheet" />
    <link href="~/js/plugin/datatables/update/css/jquery.dataTables.min.css" rel="stylesheet" />
}

@section Scripts {
    <script type="text/javascript" src="~/js/cup.js"></script>
}

